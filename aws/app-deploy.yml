apiVersion: apps/v1
kind: Deployment
metadata:
  name: spring-app
  labels:
    app: spring-app
spec:
  replicas: 2
  selector:
    matchLabels:
      app: spring-app
  template:
    metadata:
      labels:
        app: spring-app
    spec:
      containers:
        - name: spring-app
          # 👇 형님이 가져오신 완성된 이미지!
          image: gne0hy/curry:spring-7 
          ports:
            - containerPort: 8080
          env:
            # 👇 젠킨스가 여기를 AWS RDS 주소로 바꿔줄 겁니다
            - name: DB_HOST
              value: "mariadb" 
            - name: DB_USER
              value: "root"
            - name: DB_PASSWORD
              value: "test1234" # 이것도 젠킨스가 바꿔줍니다
            # (필요하다면 DB_PORT 등 추가)

---
apiVersion: v1
kind: Service
metadata:
  name: spring-app-service-aws # 이름 겹칠까봐 -aws 붙임
spec:
  type: LoadBalancer
  selector:
    app: spring-app
  ports:
    - port: 80
      targetPort: 8080
